
const ROOT = `${BASE_PATH}/`;
const LOGIN = `${BASE_PATH}/login`;
const PROJECTS = `${BASE_PATH}/projects/:id`;
const BOARDS = `${BASE_PATH}/boards/:id`;
const CARDS = `${BASE_PATH}/cards/:id`;

export default {
  ROOT,
  LOGIN,
  PROJECTS,
  BOARDS,
  CARDS,
};
